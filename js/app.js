document.addEventListener("DOMContentLoaded", () => {
  const input = document.getElementById("toReceiveResults");
  const controls = [...document.querySelectorAll(".controls")];
  const operators = [...document.getElementsByTagName('button[class="notNumbers"]')];
  const numbers = [...document.querySelectorAll(".numbers")];


 
  // controls.forEach((getElementsControls) => {
  //   getElementsControls.addEventListener("click", () => {

  //   });
  // });
});
